/**
 * Contains classes that parse a string from the user and return an instance of a client command.
 *
 * There are two grammar, a client-side grammar for commands and a server-side grammar for expressions within
 * these commands. This package contains the client-side grammar only.
 */
package gov.nasa.jpf.inspector.client.parser;