/**
 * Classes in this package concern back-stepping and they attempt to find the instruction we want to set the breakpoint at
 * when executing backstepping commands.
 */
package gov.nasa.jpf.inspector.server.pathanalysis;